import type { Actions } from './$types';

// TODO: validate form data, insert into intake_submissions.
export const actions: Actions = {
	default: async () => {
		throw new Error('not implemented');
	}
};
